name: Keep Bot Alive
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping portfolio
        run: curl -s https://mamdouhhassannew-alt.github.io/mamdouh-portfolio/ > /dev/null
